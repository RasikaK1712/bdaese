nano WordCount.java

start-dfs.sh
start-yarn.sh

jps

hdfs dfs -mkdir /exp4

nano words.txt

hdfs dfs -put words.txt /exp4

hdfs dfs -ls /exp4

mkdir classes

javac -classpath $(hadoop classpath) -d classes WordCount.java

jar -cvf wordcount.jar -C classes/ .

hdfs dfs -rm -r /output/wordcount

hadoop jar wordcount.jar WordCount /exp4/words.txt /output/wordcount

hdfs dfs -cat /output/wordcount/part-r-00000